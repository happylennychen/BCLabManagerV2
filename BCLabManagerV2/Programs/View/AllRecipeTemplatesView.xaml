<UserControl x:Class="BCLabManager.View.AllRecipeTemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:BCLabManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <UserControl.Resources>

        <LinearGradientBrush x:Key="Brush_HeaderBackground" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Color="#66000088" Offset="0" />
            <GradientStop Color="#BB000088" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ListViewItemStyle" TargetType="{x:Type ListViewItem}">
            <!-- 
      Stretch the content of each cell so that we can 
      right-align text in the Total Sales column. 
      -->
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>
    <GroupBox Header="Recipe Templates">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="39*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="60*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Recipe Template">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="_Create" Command="{Binding Path=CreateCommand}" Margin="0,5,5,0" Width="60"></Button>
                        <!--<Button Content="_Edit" Command="{Binding Path=EditCommand}" Margin="0,5,5,0" Width="60"></Button>-->
                        <Button Content="_Save As" Command="{Binding Path=SaveAsCommand}" Margin="0,5,5,0" Width="60"></Button>
                    </StackPanel>
                    <ListView Grid.Row="1"
                              x:Name="Recipelist"
              AlternationCount="2" 
              ItemContainerStyle="{StaticResource ListViewItemStyle}"
              ItemsSource="{Binding Path=AllRecipeTemplates}"
                              SelectedItem="{Binding Path=SelectedItem, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
              >

                        <ListView.View>
                            <GridView>
                                <GridViewColumn 
                                    Width="40"
                    Header="Id" 
                    DisplayMemberBinding="{Binding Path=Id}" 
                    />
                                <GridViewColumn 
                    Header="Name" 
                    DisplayMemberBinding="{Binding Path=Name}" 
                    />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>
            </GroupBox>
            <GroupBox Header="Steps" Grid.Column="2">
                <ListView Grid.Row="1"
                      x:Name="Programlist"
      AlternationCount="2"
      ItemContainerStyle="{StaticResource ListViewItemStyle}"
      ItemsSource="{Binding Path=Steps}"
      >

                    <ListView.View>
                        <GridView>
                            <GridViewColumn 
            Header="Id" 
            DisplayMemberBinding="{Binding Path=Id}" 
            />
                            <GridViewColumn 
            Header="Name" 
            DisplayMemberBinding="{Binding Path=Name}" 
            />
                            <GridViewColumn 
            Header="Loop Label" DisplayMemberBinding="{Binding Path=LoopLabel}"
            />
                            <GridViewColumn 
            Header="Loop Target" DisplayMemberBinding="{Binding Path=LoopTarget}"
            />
                            <GridViewColumn 
            Header="Loop Count" DisplayMemberBinding="{Binding Path=LoopCount}"
            />
                            <GridViewColumn 
            Header="Compare Mark" DisplayMemberBinding="{Binding Path=CompareMark}"
            />
                            <GridViewColumn 
            Header="Capacity" DisplayMemberBinding="{Binding Path=Capacity}"
            />
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>
    </GroupBox>
</UserControl>
